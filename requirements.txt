# SubtitleToolkit Desktop Application Requirements

# GUI Framework
PySide6>=6.5.0

# Existing CLI dependencies
openai>=1.0.0
anthropic>=0.18.0
python-dotenv>=1.0.0
tqdm>=4.64.0

# Additional dependencies for desktop app
# (pathlib is built-in since Python 3.4)

# Subtitle validation
pysrt>=1.1.0

# Video preview
python-mpv>=1.0.1  # Requires libmpv (install via: brew install mpv / apt install libmpv-dev / choco install mpv)

# Subtitle finder
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0  # Faster XML parser for NFO files (optional but recommended)